const projecttypes = {
    type:'internal',
    type:'external',
    type:'archived'    
  };
  
//   const roles = Object.keys(allRoles);
//   const roleRights = new Map(Object.entries(allRoles));
  
  module.exports = {
    projecttypes
  };