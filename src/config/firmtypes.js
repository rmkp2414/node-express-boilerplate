const firmtypes = {
    type:'company',
    type:'authority',
    type:'municipality'    
  };
  
//   const roles = Object.keys(allRoles);
//   const roleRights = new Map(Object.entries(allRoles));
  
  module.exports = {
    firmtypes
  };
  